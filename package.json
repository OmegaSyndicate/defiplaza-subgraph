{
  "name": "defiplaza-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ omegasyndicate/defiplaza",
    "create-local": "graph create --node http://localhost:8020/ omegasyndicate/defiplaza",
    "remove-local": "graph remove --node http://localhost:8020/ omegasyndicate/defiplaza",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 omegasyndicate/defiplaza"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
